



#include <stdio.h>

int main()
{

    int a = 24;

    char word = 'a';

    float pi = 3.14;

    double value = 28;

    printf("\nsize of int %d", sizeof(a));
    printf("\nsize of char %d", sizeof(word));
    printf("\nsize of float %d", sizeof(pi));
    printf("\nsize of double %d", sizeof(value));

    return 0;
}